{
  "name": "vouchus",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "prepare": "husky"
  },
  "lint-staged": {
    "backend/src/**/*.ts": "backend/node_modules/.bin/eslint --fix",
    "frontend/src/**/*.{ts,html}": "frontend/node_modules/.bin/eslint --fix",
    "frontend/src/**/*.scss": "frontend/node_modules/.bin/prettier --write"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0"
  }
}
